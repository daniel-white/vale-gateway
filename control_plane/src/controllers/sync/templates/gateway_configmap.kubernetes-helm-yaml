apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    vale-gateway.whitefamily.in/configmap-role: gateway-configuration
    app.kubernetes.io/name: {{ .gateway_name | quote }}
    gateway.networking.k8s.io/gateway: {{ .gateway_name | quote }}
    app: {{ .gateway_name | quote }}
  name: {{ .gateway_name }}-configuration
data:
  gateway.yaml: |
    {{ .config_yaml | nindent 4 }}

apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/name: vale-gateway
    app.kubernetes.io/instance: {{ .gateway_name | quote }}
    app.kubernetes.io/component: gateway-config
    app.kubernetes.io/managed-by: vale-gateway-controller
data:
  gateway.yaml: |
    {{- .config_yaml | nindent 4 }}